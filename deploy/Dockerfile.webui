FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV UI_PORT=5000
EXPOSE 5000

CMD ["sh", "-c", "exec gunicorn -w 2 -b 0.0.0.0:${UI_PORT} app:app"]
